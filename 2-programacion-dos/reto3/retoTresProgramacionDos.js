'use strict';

const precioKgkiwi = 2; //kilos
const precioKgPerasConferencia = 3; //kilos
const precioKgUvas = 0.5; //kilos

totalKiwi = precioKgkiwi * 5;
totalPeraConferencia = precioKgPerasConferencia * 2;
totalUvas = precioKgUvas * 4;

totalCompra = totalKiwi + totalPeraConferencia + totalUvas;

console.log("El total de su compra es de: " + totalCompra + "€");